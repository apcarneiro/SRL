Para executar os experimentos, criar o ambiente Conda com o comando:

```
conda env create -y --file environment.yml
```

Antes de executar, ativar o ambiente Conda com:

```
conda activate gymnasiumSRL
```